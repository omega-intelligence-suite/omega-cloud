[project]
id = "zotfbnjojwomjdfmaehm" # Your Project Ref

[database]
port = 54322 # Local port for dev
# OMEGA uses RLS, we ensure it's forced here
# extensions = ["pgcrypto", "pg_net"]

[auth]
site_url = "http://localhost:3000"
additional_redirect_urls = ["https://your-omega-app.vercel.app"]
enable_signup = true

[api]
port = 54321
